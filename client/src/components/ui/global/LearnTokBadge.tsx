import React from "react";

type Props = {
  status_id: string;
};

export default async function LearnTokBadge() {
  // const res = await api.getStatusByID();

  return <div className="rounded size-2 bg-red-500"></div>;
}
